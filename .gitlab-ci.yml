stages:
  - build

build-rust-latest:
  stage: build
  image: docker:latest
  variables:
    CONTAINER_NAME: grauwoelfchen/rust
    CONTAINER_TAG: latest
  services:
    - docker:dind
  before_script:
    - docker version
    - echo "$REGISTRY_PASSWORD" | docker login -u $REGISTRY_USERNAME
      --password-stdin $REGISTRY_HOSTNAME
  script:
    - cd rust
    - docker build -t $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
      latest
    - docker tag $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
      $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
    - docker push $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
  only:
    - release

build-rust-nightly:
  stage: build
  image: docker:latest
  variables:
    CONTAINER_NAME: grauwoelfchen/rust
    CONTAINER_TAG: nightly
  services:
    - docker:dind
  before_script:
    - docker version
    - echo "$REGISTRY_PASSWORD" | docker login -u $REGISTRY_USERNAME
      --password-stdin $REGISTRY_HOSTNAME
  script:
    - cd rust
    - docker build -t $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
      nightly
    - docker tag $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
      $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
    - docker push $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
  only:
    - release

build-rust-ci-tools-nightly:
  stage: build
  image: docker:latest
  variables:
    CONTAINER_NAME: grauwoelfchen/rust-ci-tools
    CONTAINER_TAG: nightly
  services:
    - docker:dind
  before_script:
    - docker version
    - echo "$REGISTRY_PASSWORD" | docker login -u $REGISTRY_USERNAME
      --password-stdin $REGISTRY_HOSTNAME
  script:
    - cd rust-ci-tools
    - docker build -t $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
      nightly
    - docker tag $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
      $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
    - docker push $REGISTRY_HOSTNAME/$CONTAINER_NAME:$CONTAINER_TAG
  only:
    - release
